

# Async Storage => It is a react native simple promise based api for persisting small bits of data on a user's device. Think of it as the mobile app equivalent of the browser's local, but async and cross-plat.
<!-- export default function RootLayout() {
  return (
    <Stack screenOptions={{
      headerShown: false
    }}>
      {/* <Stack.Screen
        name="index"
        options={{
          title: "Home",
        }}
      /> */}
      {/* <Stack.Screen
        name="about"
        options={{
          title: "About",
        }}
      /> */}

      <Stack.Screen
        name="(tabs)"
        options={{
          title: "Home",
        }}
      />
    </Stack>
  );
}

 -->
